export enum SpecialValues {
   Unspecified = "unspecified",
}

export enum Routes {
   Home = '/',
   Projects = 'projects',
   Executors = 'executors',
   Positions = 'positions',
   Create = 'create',
   Options = 'options',
   Tasks = 'tasks',
}